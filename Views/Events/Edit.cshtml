@model EventFormViewModel
@{
    ViewData["Title"] = "Edit Event";
}

<section class="form-shell">
    <div class="form-header">
        <div>
            <p class="eyebrow">Edit event</p>
            <h1>Adjust the programme details.</h1>
        </div>
        <a class="button-secondary" asp-controller="EventsPage" asp-action="Index">Back to list</a>
    </div>

    <div asp-validation-summary="All"></div>

    <form asp-controller="EventsPage" asp-action="Edit" asp-route-id="@Model.Id" method="post" enctype="multipart/form-data" class="grid-form">
        <input asp-for="Id" type="hidden" />
        <input asp-for="ImageUrl" type="hidden" />
        <div class="field">
            <label asp-for="Name"></label>
            <input asp-for="Name" />
            <span asp-validation-for="Name"></span>
        </div>
        <div class="field">
            <label asp-for="ImageFile"></label>
            <input asp-for="ImageFile" type="file" accept=".jpg,.jpeg,.png,.webp,.gif" />
            <span asp-validation-for="ImageFile"></span>
        </div>
        <div class="field-wide">
            <label asp-for="Description"></label>
            <textarea asp-for="Description"></textarea>
            <span asp-validation-for="Description"></span>
        </div>
        <div class="field">
            <label asp-for="StartDateTime"></label>
            <input asp-for="StartDateTime" type="datetime-local" />
            <span asp-validation-for="StartDateTime"></span>
        </div>
        <div class="field">
            <label asp-for="EndDateTime"></label>
            <input asp-for="EndDateTime" type="datetime-local" />
            <span asp-validation-for="EndDateTime"></span>
        </div>
        @if (!string.IsNullOrWhiteSpace(Model.ImageUrl))
        {
            <div class="field-wide">
                <span class="eyebrow">Current image</span>
                <img class="image-preview image-preview-compact" src="@Model.ImageUrl" alt="@Model.Name" />
            </div>
        }
        <div class="field-wide">
            <div class="button-row">
                <button type="submit" class="button-primary">Update Event</button>
                <a class="button-secondary" asp-controller="EventsPage" asp-action="Details" asp-route-id="@Model.Id">View Details</a>
            </div>
        </div>
    </form>
</section>
